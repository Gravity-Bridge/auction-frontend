/**
 * Endpoint configuration for RPC and REST endpoints
 * Using public gravitychain.io endpoints (no API key required)
 */

/**
 * Default RPC endpoint - public gravitychain.io endpoint
 */
export const DEFAULT_RPC_ENDPOINT = "https://gravitychain.io:26657";

/**
 * Default REST endpoint - public gravitychain.io endpoint
 */
export const DEFAULT_REST_ENDPOINT = "https://gravitychain.io:1317"; 